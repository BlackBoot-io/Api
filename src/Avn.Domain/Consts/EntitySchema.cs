namespace Avn.Domain.Consts;

public static class EntitySchema
{
    public const string Admin = nameof(Admin);
    public const string Base = nameof(Base);
    public const string Auth = nameof(Auth);
}